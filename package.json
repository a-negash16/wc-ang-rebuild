{
  "name": "wc-ang-rebuild",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Parallel rebuild for WC ANG prediction league.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "test": "node --test",
    "hash-pin": "node scripts/hash-pin.mjs",
    "set-manager-pin": "node scripts/set-manager-pin.mjs",
    "smoke:prediction": "node scripts/smoke-prediction-flow.mjs",
    "supabase:bundle": "node scripts/build-supabase-sql.mjs",
    "supabase:check": "node scripts/check-supabase.mjs",
    "check": "node --check src/rules/scoring.js && node --check src/rules/predictions.js && node --check src/integrations/odds-api.js && node --check src/lib/auth/pin.js && node --check src/lib/auth/session.js && node --check src/data/league.js && node --test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "next": "^14.2.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-next": "^14.2.0"
  },
  "engines": {
    "node": ">=20"
  }
}
